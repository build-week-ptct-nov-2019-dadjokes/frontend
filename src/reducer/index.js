export const initialState = {
  jokes: [],
  isLoading: false,
  error: "",
  loggedIn: false
};

export const reducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};
